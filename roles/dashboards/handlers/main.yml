---
# systemctl restart dashboards --now
- name: restart dashboards
  ansible.builtin.systemd: name=dashboards state=restarted enabled=true

# systemctl restart nftables --now
- name: Restart nftables
  ansible.builtin.service:
    name: nftables
    state: restarted
    enabled: yes
